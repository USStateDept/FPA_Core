DO $$
BEGIN
	RAISE INFO 'CREATE of INDICATOR FOREIGN CONSTRAINTS';

	ALTER TABLE FIND.INDICATOR
		OWNER TO finddbuser;

	ALTER TABLE FIND.INDICATOR ADD CONSTRAINT CATEGORY_ID_FK1
		FOREIGN KEY (CATEGORY_ID)
		REFERENCES FIND.CATEGORY(CATEGORY_ID) MATCH SIMPLE
		ON UPDATE NO ACTION ON DELETE NO ACTION;

END $$;
