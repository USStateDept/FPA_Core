DO $$
BEGIN
	RAISE INFO 'CREATE of COUNTRY_ALTNAME FOREIGN CONSTRAINTS';

	ALTER TABLE FIND.COUNTRY_ALTNAME_LKUP
		OWNER TO finddbuser;

	ALTER TABLE FIND.COUNTRY_ALTNAME_LKUP ADD CONSTRAINT COUNTRY_ID_FK1
		FOREIGN KEY (COUNTRY_ID)
		REFERENCES FIND.COUNTRY(COUNTRY_ID) MATCH SIMPLE
		ON UPDATE NO ACTION ON DELETE NO ACTION;

END $$;
